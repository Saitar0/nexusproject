{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:path:default",
    "core:window:default",
    "core:event:default",
    "core:app:default",
    "opener:default",
    "shell:default",
    "updater:default",
    "process:default"
  ]
}
